import math
class Circle:
    def __init__(self,radius):
        self.radius = radius
        self.a = (math.pi * math.pow(self.radius,2))
    def __init__(self,perimeter):
        self.perimeter = perimeter
        self.b = 2 * math.pi * c1(self.radius)
c1 = Circle(8)
print(c1.a)
print(c1.b)
